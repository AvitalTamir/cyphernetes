{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "operator-test-setup",
            "type": "shell",
            "command": "cd ${workspaceFolder}/operator && make manifests generate fmt vet envtest",
            "problemMatcher": [
                "$go"
            ]
        }
    ]
}