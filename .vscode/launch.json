{
    "version": "0.8.0",
    "configurations": [
        {
            "name": "Configure",
            "type": "go",
            "request": "launch",
            "mode": "debug",
            "program": "${workspaceFolder}/cmd/cyphernetes",
            "args": ["shell"],
            "console": "integratedTerminal"
        }
    ]
}